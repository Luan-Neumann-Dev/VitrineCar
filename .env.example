# Identidade da loja (aparece no cabecalho, rodape e nas mensagens do WhatsApp)
NEXT_PUBLIC_STORE_NAME="Almeida Veículos"
NEXT_PUBLIC_STORE_TAGLINE="Revenda de carros seminovos · Campinas, SP"
NEXT_PUBLIC_STORE_HOURS="Atendimento de seg a sáb, 9h às 19h"
NEXT_PUBLIC_WHATSAPP_NUMBER="5519998877665"

# Dominio publico do site — usado em canonical, sitemap e dados estruturados
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Dominio publico do bucket R2 com as fotos (fase 2)
NEXT_PUBLIC_PHOTOS_BASE_URL=""

# --- Segredos do painel (NAO levam NEXT_PUBLIC: ficam so no servidor) ---
# Em producao vao por `wrangler secret put NOME`, nunca em arquivo versionado.
# Senha unica de acesso ao painel do vendedor
ADMIN_PASSWORD=""
# Chave que assina o cookie de sessao. Gere com:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
ADMIN_SESSION_SECRET=""
