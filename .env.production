# Só o que muda em produção. O resto vem do .env.
# Mesma regra: apenas NEXT_PUBLIC_*, nunca segredo.

NEXT_PUBLIC_SITE_URL="https://vitrine-carros.lnneumann.workers.dev"

# Domínio próprio do bucket R2. Vazio = as fotos saem pela própria aplicação,
# que já funciona. Ver passo 10 do README.
NEXT_PUBLIC_PHOTOS_BASE_URL=""
